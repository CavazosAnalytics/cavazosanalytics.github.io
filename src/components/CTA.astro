---
const { title = 'Ready to talk?', body = 'Book a consult and get actionable insights.', href = '/contact', cta = 'Book consult' } = Astro.props;
---
<section class="my-16 p-8 rounded-2xl bg-[var(--panel)] shadow-soft">
  <h2 class="text-2xl font-semibold">{title}</h2>
  <p class="mt-2 text-[var(--muted)]">{body}</p>
  <a href={href} class="inline-flex mt-6 px-5 py-3 rounded-2xl bg-blue-into text-white">{cta}</a>
</section>
